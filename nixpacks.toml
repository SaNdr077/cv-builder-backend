[phases.setup]
nixPkgs = ["chromium", "nss", "atk", "cups", "dbus", "libdrm", "libxkbcommon", "mesa"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "playwright install chromium"
]

[start]
cmd = "gunicorn cv_builder.wsgi"

